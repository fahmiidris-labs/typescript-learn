describe('Data Type', function () {
  it('should must desclare', function () {
    const name: string = 'Fahmi Idris';
    const balance: number = 1_000_000;
    const isVIP: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVIP);
  });
});
